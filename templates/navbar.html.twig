<!DOCTYPE html>
<html>
    <nav>
        <ul>
            <li>
                <a href={{ path('app_index') }}>Home</a>
            </li>
            <li>
                <a href={{ path('app_bar') }}>Nos bars</a>
            </li>
            <li>
                <a href={{ path('app_artist') }}>Les artistes</a>
            </li>
            {% if app.user %}
            <li>
                <a href={{ path('app_bar_new')}}>Create a bar</a>
            </li>
            {% endif %}
            {% if app.user %}
            <li>
                <a href={{ path('app_artist_new')}}>Create an artist</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
            <li>
                <a href={{ path('admin')}}>CRUD DASHBOARD</a>
            </li>
            {% endif %}
            <li>
                {% if app.user %}
                <a href={{ path('app_logout') }}>Logout</a>
                {% else %}
                <a href={{ path('app_login') }}>Login</a>
                {% endif %}
            </li>
            
         </ul>
    </nav>
</html>
