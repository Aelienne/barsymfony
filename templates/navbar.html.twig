<!DOCTYPE html>
<html>
    <nav>
        <ul>
            <li>
                <a href={#"{{ path('app_index') }}"#}>Accueil</a>
            </li>
            <li>
                <a href={#"{{ path('categories_list') }}"#}>Nos bars</a>
            </li>
            <li>
                <a href={#"{{ path('articles_list') }}"#}>Les artistes</a>
            </li>
            <li>
                <a href={#"{{ path('newsletter_subscribe') }}"#}>Newsletter</a>
            </li>
            <li>
                <a href={#"{{ path('') }}"#}>Login</a>
            </li>
         </ul>
    </nav>
</html>
