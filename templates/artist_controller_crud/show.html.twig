{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Nickname</th>
                <td>{{ artist.nickname }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ artist.picture }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ artist.description }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ artist.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ artist.lastName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ artist.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ artist.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_artist_controller_crud_index') }}">back to list</a>

    <a href="{{ path('app_artist_controller_crud_edit', {'id': artist.id}) }}">edit</a>

    {{ include('artist_controller_crud/_delete_form.html.twig') }}
{% endblock %}
